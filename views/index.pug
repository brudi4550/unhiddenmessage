<!DOCTYPE html>
html
  head
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title= title
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous")
    link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Montserrat")
    link(rel="stylesheet" href="css/styles.css")
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css")
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous")
    script(src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/1.4.1/p5.min.js")
  body
    nav.navbar.navbar-dark.bg-dark.navbar-expand-md
      div.container-fluid
        a.navbar-brand.abs#navbar-brand(href='#') unhiddenmessage
        i.bi.bi-question-circle.nav-item.ms-auto.clickable#modal-button
    .container
      .row
        .col.my-auto.text-center
          form#form(role='form' method='POST' action='/')
            textarea(placeholder=placeholder_message name='message' cols=30 rows=2)
            br
            button.btn(type='submit' name='submit' id='submit') Create something unique
        .col.text-center#canvas-container
          #canvas.fade-in-image.text-center
          script(id="masterpiece" src="js/unhide_message.js", data-hash=hash)
        .col.my-auto.text-center
          h1#banner-msg a message hidden in plain sight
      .col-md-12.text-center#quote
        h3 "#{quote.quote}" - #{quote.quotee}
    div#modal
      div#modal-content
        span#close-modal
          i.bi.bi-x-lg 
        p <b>Given the same message the resulting art will always be the same. 🖌</b>
        p This website uses no cookies, stores nothing about what you type in
          |, has no database and only a small backend server. It is as dumb as it gets,
          | just how it should be. Have fun using it. 😊
        p If you want to find out more about what I do, here are some links: 
        a(href="https://github.com/brudi4550")
          i.bi.bi-github  My GitHub 
        br
        a(href="http://www.alexanderwolf.xyz")
          i.bi.bi-person-heart  My personal website (coming soon)
        br
        br
        p.text-right Alexander Wolf © 2022
        p If you are using Safari you will get different results since it doesn't support bluring the canvas in javascript.
    script(src="js/main.js")

